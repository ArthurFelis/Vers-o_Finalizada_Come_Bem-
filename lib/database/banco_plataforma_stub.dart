/// Implementação vazia (fallback). Nunca deve ser escolhida em prática --
/// serve só pra satisfazer o import condicional em `banco_plataforma.dart`
/// caso nenhuma das duas plataformas (io/web) seja detectada.
void configurarBancoDeDados() {}
